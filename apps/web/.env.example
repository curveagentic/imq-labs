# Local dev: standalone Express on :4000.
# In production on Netlify, leave NEXT_PUBLIC_API_URL EMPTY so the frontend
# calls the same origin (Netlify rewrites /api/* to the API Function).
NEXT_PUBLIC_API_URL=http://localhost:4000
